<?xml version="1.0"?>

<!-- This must match the product descriptor except for the format. -->
<assembly>
	<id>install</id>
	<formats>
		<format>dir</format>
	</formats>
	<includeBaseDirectory>false</includeBaseDirectory>
	<fileSets>
		<fileSet>
			<directory>source/main/assembly/resources</directory>
			<outputDirectory>/bin</outputDirectory>
			<includes>
				<!-- TODO Rename and add later -->
				<!--
				<include>program.exe</include>
				<include>program.ini</include>
				<include>program32.exe</include>
				<include>program64.exe</include>
				<include>program.reg</include>
				<include>program.ico</include>
				-->
				<include>program.png</include>
			</includes>
		</fileSet>
	</fileSets>
	<dependencySets>
		<dependencySet>
			<outputFileNameMapping>${artifact.artifactId}.${artifact.extension}</outputFileNameMapping>
			<outputDirectory>/lib</outputDirectory>
			<scope>runtime</scope>
		</dependencySet>
	</dependencySets>
</assembly>
