<?xml version="1.0" encoding="UTF-8" standalone="yes" ?>

<izpack:shortcuts version="5.0" xmlns:izpack="http://izpack.org/schema/shortcuts" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://izpack.org/schema/shortcuts http://izpack.org/schema/5.0/izpack-shortcuts-5.0.xsd">

	<programGroup
		defaultName="${project.name}"
		location="applications"
	/>

	<shortcut
		name="${project.name}"
		programGroup="yes"
		desktop="yes"
		applications="yes"
		startMenu="no"
		startup="no"
		target="java"
		commandLine="-p ${INSTALL_PATH}/lib -m com.xeomar.xenon/com.xeomar.xenon.Product"
		workingDirectory="${INSTALL_PATH}"
		description="${project.name} application framework."
		iconFile="${INSTALL_PATH}/bin/program.png"
		iconIndex="0"
		type="Application"
		encoding="UTF-8"
		initialState="noShow"
		categories="Application;"
	/>

	<!--
	<shortcut
		name="${project.name} Uninstall"
		programGroup="yes"
		desktop="no"
		applications="no"
		startMenu="no"
		startup="no"
		target="java"
		commandLine="-jar ${INSTALL_PATH}/lib/remove.jar"
		initialState="noShow"
		iconFile="trashcan_full"
		iconIndex="0"
		workingDirectory="$USER_HOME"
		type="Application"
		encoding="UTF-8"
		KdeSubstUID="true"
		KdeUsername="root"
		createForAll="false"
		categories="Application;"
		tryexec="java -jar ${INSTALL_PATH}/lib/remove.jar"
		description="Uninstall ${project.name} application framework."
	/>
	-->

</izpack:shortcuts>
