package com.parallelsymmetry.essence;

import javafx.scene.paint.Color;

public class Colors {

	public static Color getShade( Color color, double range ) {
		if( range < 0 ) range = 0;
		if( range > 1 ) range = 1;

		// NEXT Work on color methods

		return color;
	}

}

