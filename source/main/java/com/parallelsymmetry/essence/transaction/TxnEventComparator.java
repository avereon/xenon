package com.parallelsymmetry.essence.transaction;

import java.util.Comparator;

public class TxnEventComparator implements Comparator<TxnEvent> {

	@Override
	public int compare( TxnEvent event1, TxnEvent event2 ) {
		// TODO Compare transaction events to determine which to send first

		// What should TxnEvent have to compare???

		return 0;
	}

}
