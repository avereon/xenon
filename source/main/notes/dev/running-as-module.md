# Running ${project.name} as a Java Module

java -p <module path> -m <module>

java -p $APP_HOME/lib -m com.avereon.xenon/com.avereon.xenon.Launcher

or should the modules be in a mod folder

java -p $APP_HOME/mod -m com.avereon.xenon/com.avereon.xenon.Launcher
